# Gaudi Rules

> Generated by `gaudi cheat-sheet`. Do not edit by hand.
> Source: `src/gaudi/packs/` — change the rule's `recommendation_template` field, then regenerate.

## Errors

- **ALM-OPS-001 MultipleHeads** [alembic] — Merge divergent migration branches with 'alembic merge heads'. Multiple heads cause 'alembic upgrade head' to fail.
- **ARCH-010 ImportDirectionViolation** — Import direction should flow inward only. Outer imports middle, middle imports inner. Never reverse.
- **ASYNC-001 SharedMutableStateAcrossThreads** — Move shared state into a thread-safe container (queue.Queue, threading.Lock-guarded dict) or return values from the worker and merge on the main thread. Concurrent mutation of plain dict/list/set is a race condition.
- **DEP-001 CircularImport** — Break the cycle by extracting shared code into a separate module, using dependency inversion, or deferring imports. Circular imports cause ImportError at runtime in Python.
- **DJ-ARCH-004 TransactionBoundaryViolation** [django] — Network I/O inside an atomic block holds row/table locks for the duration of the request. Commit the transaction first, then make the call -- or use on_commit() if the call must follow a successful commit.
- **DJ-SEC-001 DjangoSecretKeyExposed** [django] — Load SECRET_KEY from environment variables: SECRET_KEY = os.environ['SECRET_KEY']. Hardcoded secrets in source control are a critical security vulnerability.
- **DJ-SEC-002 DjangoDebugTrue** [django] — Set DEBUG = False in production. Use environment variables to control debug mode.
- **HTTP-SCALE-001 RequestsNoTimeout** [requests] — Always set a timeout on HTTP requests. Without a timeout, your application can hang indefinitely waiting for a response.
- **PY314-001 RemovedIn314Import** — {replacement}
- **PY314-005 NotImplementedBoolContext** — Return NotImplemented from special methods to signal the operation is not supported, but never use it in if/while conditions or bool() calls. Use 'is NotImplemented' for checks.
- **PYD-ARCH-001 PydanticMutableDefault** [pydantic] — Use Field(default_factory=list) instead of Field(default=[]) in Pydantic models. Mutable defaults are shared across instances.
- **SEC-002 RawSQLInjection** — Use parameterized queries: pass values as a separate tuple/dict argument to execute() instead of formatting them into the SQL string.
- **SEC-003 HardcodedCredential** — Read secrets from environment variables (os.getenv) or a secrets manager. Hardcoded credentials leak through git history.
- **SEC-004 EvalExecUsage** — eval() and exec() execute arbitrary code. Use ast.literal_eval for safe expression parsing, or refactor to avoid dynamic execution.
- **SEC-005 UnsafeDeserialization** — pickle and yaml.load can execute arbitrary code on untrusted input. Use json, or yaml.safe_load / Loader=SafeLoader.
- **SMELL-005 GlobalData** — Avoid mutable module-level variables. Use function-local state, dependency injection, or frozen data structures.
- **SMELL-006 MutableData** — Avoid shared mutable state. Pass data as function parameters and return results.
- **STAB-005 BlockingInAsync** — Use async equivalents (asyncio.sleep, httpx.AsyncClient) in async functions. Blocking calls freeze the event loop and starve all concurrent tasks.
- **STRUCT-010 PathHacks** — Use proper packaging (pyproject.toml + pip install -e .) instead of sys.path hacks.

## Warnings

- **ALM-ARCH-001 MigrationNoDowngrade** [alembic] — Implement downgrade() so migrations can be rolled back. Irreversible migrations block emergency rollbacks in production.
- **API-001 MissingPagination** — Slice the queryset (e.g. queryset[offset:offset+limit]), wrap it in a Paginator, or use a framework pagination class. Unbounded list endpoints are a latency and memory time bomb.
- **API-002 InconsistentReturnType** — Pick one return shape per endpoint -- either always return a dict (and let the framework serialize) or always return a Response object. Mixed shapes confuse clients and OpenAPI generators.
- **ARCH-002 GodModel** — Models with more than {threshold} fields often contain distinct concerns. Consider extracting related fields into separate models with OneToOneField relationships.
- **ARCH-011 ConnectorLogicLeak** — Connectors should only talk to external systems. Move decision-making to the service layer.
- **ARCH-013 FatScript** — Entry points should be thin — parse input, call a service, format output. Move logic to service functions.
- **ARCH-020 EnvLeakage** — Accept configuration through __init__ parameters. Only factory functions should read os.getenv().
- **ARCH-022 ScatteredConfig** — Centralize configuration reading in a single module. Other modules should receive config via parameters.
- **ASYNC-002 MissingAsyncContextManager** — Wrap async HTTP clients in 'async with': they hold a connection pool that leaks unless the session is explicitly closed.
- **ASYNC-003 MixedSyncAsyncModule** — Pick one: use httpx.AsyncClient/aiohttp throughout the module, or remove the async def. Sync HTTP inside an async module blocks the event loop.
- **AWS-ARCH-001 HardcodedRegion** [boto3] — Inject region via configuration or environment variable instead of hardcoding. Hardcoded regions make multi-region deployment impossible.
- **AWS-ERR-001 BareClientCall** [boto3] — Wrap boto3 API calls in try/except for botocore.exceptions.ClientError. AWS calls fail for many reasons (permissions, throttling, network).
- **AWS-SCALE-001 UnpaginatedList** [boto3] — Use client.get_paginator('{method}') instead of calling {method} directly. Without pagination, results are truncated at AWS default limits.
- **CELERY-ARCH-001 CeleryNoRetry** [celery] — Add autoretry_for, max_retries, and retry_backoff to tasks that call external services. Tasks without retry config fail permanently on transient errors.
- **CELERY-SCALE-001 CeleryNoTimeLimit** [celery] — Add time_limit and soft_time_limit to prevent runaway tasks from consuming workers.
- **CPLX-002 PassThroughVariable** — Pass-through variables signal a wrong abstraction boundary. Consider grouping the threaded values into a context object, or moving the work that needs '{param}' down to the layer that already has it.
- **CPLX-003 InformationLeakage** — Information leakage couples callers to implementation details. Either widen '{leaked}' to a public type/protocol or make '{func}' itself private. Ousterhout: 'each module should hide information about how it works.'
- **DEP-002 FanOutExplosion** — Split this module into smaller, focused modules. High fan-out means a module has too many responsibilities and is fragile to changes in any dependency.
- **DEP-004 UnstableDependency** — Stabilize this module by reducing its outgoing dependencies, or depend on an abstraction instead. Per the Stable Dependencies Principle, modules that others depend on should be stable (low instability).
- **DJ-ARCH-001 BusinessLogicInSerializer** [drf] — Serializers convert between wire format and domain objects. Orchestration (pricing, fulfilment, notifications) belongs in a service callable from the view, not inside create()/update().
- **DJ-ARCH-002 BusinessLogicInSignal** [django] — Signals are best for notification, not workflow. A handler that branches, sends mail, and calls external services hides causality. Have the view call a service explicitly instead.
- **DJ-ARCH-003 ModelCallsExternalService** [django] — A model method may compute or persist its own state, but it must not call mail / HTTP / task queues. Move the side effect to a service that orchestrates the model and the external call.
- **DOM-001 AnemicDomainModel** — Move the business rules that operate on this data into methods on the model itself. A model that only holds fields forces logic to scatter across services and views.
- **DOM-002 WrongLayerPlacement** — Move the branching decision into a service or model method and have the view delegate to it. Views should parse input and format output, not decide policy.
- **DRF-SEC-001 DRFNoPermissionClass** [drf] — Set permission_classes on every ViewSet. Relying on DEFAULT_PERMISSION_CLASSES is fragile — a settings change could expose all endpoints.
- **ERR-001 BareExcept** — Catch specific exceptions. If you must catch broadly, re-raise after handling.
- **ERR-002 BroadTryBlock** — Wrap only the operation that can fail in try. Move setup and post-processing outside the try block.
- **ERR-003 ErrorSwallowing** — Logging an error without re-raising hides failures. Log and re-raise, or handle the error explicitly.
- **ERR-004 ExceptPass** — Log, handle, or re-raise the exception. `pass` makes failures invisible.
- **ERR-006 ExceptionInInit** — Move side effects out of __init__ into a classmethod factory, or restrict __init__ to ValueError/TypeError on bad arguments.
- **FAPI-ARCH-001 FastAPINoResponseModel** [fastapi] — Add response_model parameter to endpoints for automatic validation, serialization, and OpenAPI documentation.
- **FLASK-STRUCT-001 FlaskNoAppFactory** [flask] — Use create_app() factory function instead of module-level Flask(). Factories enable testing, multiple configs, and blueprint registration.
- **IDX-001 MissingStringIndex** — Add db_index=True to '{column}' or include it in a composite index.
- **LLM-ARCH-001 HardcodedModel** [anthropic] — Extract model names to a configuration constant or environment variable. Hardcoded model strings make it difficult to update models across the codebase.
- **LLM-ERR-001 BareAPICall** [anthropic] — Wrap anthropic API calls in try/except to handle APIError, RateLimitError, and APIConnectionError. Bare calls will crash on transient failures.
- **LLM-SCALE-001 NoTokenCounting** [anthropic] — Check prompt token length before making API calls. Without token counting, oversized prompts will be rejected by the API and waste latency.
- **LOG-002 SensitiveDataInLog** — Logs leak. Never include passwords, tokens, API keys, SSNs, or other credentials in log records -- not as positional args, not in f-strings, and not as `extra=` fields. Log an opaque user id or hashed handle instead.
- **LOG-004 PrintInsteadOfLog** — Service code should emit log records, not stdout text. Replace print() with logger.info / logger.debug so the message gains a level, a logger name, and structured fields the operator can filter on.
- **OPS-006 DockerfileAntiPattern** — Pass --no-cache-dir to pip install, and COPY dependency manifests (requirements.txt, pyproject.toml, poetry.lock) before COPY . . so the dependency-install layer is cached across source changes.
- **OPS-008 HardcodedPortOrHost** — Read host and port from configuration (env var, settings module), not from a literal. Hardcoded binds resist deployment to staging, containers, or alternate ports and hide the deploy contract from the reader.
- **PD-ARCH-001 PandasInplaceAntiPattern** [pandas] — Use df = df.method() instead of df.method(inplace=True). inplace breaks method chaining and is planned for deprecation.
- **PD-SCALE-001 PandasIterrows** [pandas] — iterrows() is extremely slow. Use .apply(), vectorized operations, or .itertuples() (10-100x faster) instead.
- **PY314-002 DeprecatedIn314Import** — {replacement}
- **PY314-003 DeferredAnnotationAccess** — Use annotationlib.get_annotations() or typing.get_type_hints() instead of directly accessing __annotations__. Deferred annotations may not be evaluated yet.
- **PY314-004 FinallyControlFlow** — Move the {statement} statement outside the finally block. Control flow in finally blocks will become a SyntaxError in a future Python version.
- **PY314-006 TarfileNoFilter** — Add filter='data' (safe default) or filter='fully_trusted' (old behavior) to tarfile.extract() and tarfile.extractall() calls.
- **SA-SCALE-001 SQLAlchemyLazyDefault** [sqlalchemy] — Explicitly set lazy='select', 'joined', 'subquery', or 'selectin' on relationships. Default lazy loading causes N+1 queries.
- **SCHEMA-002 ColumnSprawl** — Review nullable columns. If groups of nullable fields are only populated together, they likely belong in a separate related model.
- **SEC-006 SSRFVector** — Validate URLs against an allowlist before making HTTP requests. Use urlparse() to inspect scheme and hostname, and restrict to known-safe hosts.
- **SMELL-002 DuplicatedCode** — Extract the shared logic into a single function parameterized by the differences.
- **SMELL-003 LongFunction** — Extract smaller functions. Long functions are hard to understand, test, and reuse.
- **SMELL-004 LongParameterList** — Introduce a parameter object or split the function. Long parameter lists are hard to understand.
- **SMELL-007 DivergentChange** — Split the class into focused classes, each handling one set of related attributes.
- **SMELL-008 ShotgunSurgery** — Centralize usage behind a single function or class to reduce the blast radius of changes.
- **SMELL-009 FeatureEnvy** — Move this method to the class it's most interested in.
- **SMELL-011 PrimitiveObsession** — Replace string comparisons with an Enum. Enums catch typos at definition time.
- **SMELL-012 RepeatedSwitches** — Replace repeated switches with polymorphism or a dispatch dictionary.
- **SMELL-015 SpeculativeGenerality** — {advice}
- **SMELL-016 TemporaryField** — Temporary fields suggest the class has multiple responsibilities. Extract into separate classes or use method-local variables.
- **SMELL-018 MiddleMan** — If a class mostly delegates, remove it and use the underlying object directly.
- **SMELL-019 InsiderTrading** — Ask objects to modify themselves instead of reaching into their internals.
- **SMELL-020 LargeClass** — Large classes have too many responsibilities. Extract related methods into separate classes.
- **SMELL-023 RefusedBequest** — If a subclass doesn't want parent behavior, prefer composition over inheritance.
- **STAB-001 UnboundedResultSet** — Add .limit(), slicing [:n], or pagination to ORM queries. Unbounded result sets grow with data and eventually exhaust memory.
- **STAB-003 RetryWithoutBackoff** — Add exponential backoff to retry logic. Retries without backoff create thundering herds that amplify failures.
- **STAB-004 UnboundedCache** — Set a maxsize on @lru_cache or replace @cache with @lru_cache(maxsize=N). Unbounded caches grow until they exhaust memory.
- **STAB-006 UnmanagedResource** — Use a 'with' statement for resources that need cleanup. Unmanaged resources leak file handles, connections, or sessions on exceptions.
- **STAB-007 UnboundedThreadPool** — Set max_workers on ThreadPoolExecutor. Without a bound, thread creation scales with demand and can exhaust system resources under load.
- **STAB-008 IntegrationPointNoFallback** — Wrap external HTTP calls in try/except so the caller can degrade. An integration point with no fallback propagates every upstream failure.
- **STAB-009 FailFastLateValidation** — Move isinstance/value-range checks to the public entry point. Validation in private helpers means callers fail deep in the stack instead of at the boundary.
- **STRUCT-001 SingleFileModels** — Convert models.py into a models/ package with separate files per domain. Create models/__init__.py that imports all models for Django compatibility.
- **STRUCT-011 MissingPyproject** — Add a pyproject.toml for modern Python packaging, dependency management, and tool configuration.
- **STRUCT-012 NoEntryPoint** — Register this script as a console_scripts entry point in pyproject.toml.
- **STRUCT-021 MagicStrings** — Extract repeated string literals into named constants or enums.
- **SVC-001 HardcodedServiceURL** — Inject service URLs via configuration (environment variables or config files). Hardcoded URLs break when services move, scale, or deploy to new environments.
- **SVC-004 SharedDatabasePattern** [django] — Two services reading the same table is a hidden contract no one signed. Expose the data through the owning app's service layer (a function or API) so the schema can change without breaking unrelated apps.
- **SVC-005 SynchronousCouplingChain** — Sequential sync calls to multiple services tie the caller's latency and reliability to the slowest peer. Fan out in parallel (asyncio.gather, a thread pool) or pull the data through a single aggregating endpoint.
- **TEST-ARCH-001 PytestOverMocking** [pytest] — Five or more mocks usually means the test is exercising the mocks, not the code. Extract collaborators behind a seam, or write an integration test that uses real objects.
- **TEST-ARCH-003 PytestAssertInProductionCode** — Replace `assert <cond>` with `if not <cond>: raise ValueError(...)` (or another explicit exception). Asserts are for tests and internal debug-mode invariants, not for input validation.
- **TEST-STRUCT-003 PytestTestMethodTooLong** [pytest] — A test should exercise one behaviour. Split the function along its arrange/act/assert seams, or extract setup into a fixture.

## Info

- **API-003 LeakingInternalID** — Replace <int:pk> with <uuid:pk> or a slug. Sequential integer IDs leak row counts and enable BOLA-style enumeration attacks.
- **API-004 NoErrorResponseSchema** — Add a responses={{404: {{'model': Error}}, ...}} kwarg so the OpenAPI document describes both the success and error shapes.
- **ARCH-001 NoTenantIsolation** — Consider adding a tenant_id, organization_id, or account_id ForeignKey to models that store user data. Enforce filtering in all queries. Promote to warn with [gaudi.rules] "ARCH-001" = "warn" if multi-tenancy applies.
- **ARCH-003 NullableForeignKeySprawl** — Review whether nullable ForeignKeys represent truly optional relationships or if the model is trying to handle multiple relationship types. Consider a join table or polymorphic pattern.
- **ASYNC-004 NoGracefulShutdown** — Register a signal.signal(SIGTERM, ...) handler before asyncio.run() so the service can drain in-flight work on shutdown.
- **CPLX-001 ShallowModule** — Deepen the module by combining trivial helpers or hiding them behind fewer, richer entry points. Ousterhout: 'modules should be deep' -- the cost of a public name is the documentation and cognitive load it imposes on every caller.
- **CPLX-004 ConjoinedMethods** — Conjoined methods force callers to remember a specific call order. Combine '{setter}' and '{checker}' into one operation, use a context manager, or pass the state explicitly so the order is impossible to get wrong.
- **DEP-003 FanInConcentration** — Consider whether this module is doing too much. A module imported everywhere becomes a fragile hub — any change risks breaking the entire project.
- **DOM-003 ActiveRecordMisuse** — Extract the external call into a service that takes the model as input. Persistence and orchestration are separate responsibilities.
- **DRF-SCALE-001 DRFNoThrottling** [drf] — Add throttle_classes to prevent abuse. Public APIs without rate limiting are vulnerable to DDoS and abuse.
- **ERR-005 InconsistentExceptions** — Define a module-level base exception (e.g. `class FooError(Exception)`) and have specific failures inherit from it so callers can catch one type.
- **IDX-002 NoIndexOnFilterableField** — If you query or sort by '{column}', add db_index=True. Date fields are among the most commonly filtered columns.
- **LOG-001 UnstructuredLogging** — Use logger.info('message %s', var) instead of logger.info(f'message {{var}}'). Lazy formatting avoids string construction when log level is disabled.
- **LOG-003 InconsistentLoggerName** — logging.getLogger(__name__) lets the logger inherit configuration from its package and respects the dotted hierarchy. Hardcoded names break that and make per-module log filtering brittle.
- **LOG-005 NoCorrelationID** — Pass extra={{'request_id': request_id}} (or correlation_id / trace_id) on every logger call inside an HTTP handler so log lines from the same request can be stitched together by ops.
- **OPS-002 MissingPrecommit** — Add pre-commit hooks for automated code quality checks before each commit.
- **OPS-003 MissingPRTemplate** — Add .github/PULL_REQUEST_TEMPLATE.md to enforce consistent PR descriptions with summary, motivation, and test plan sections.
- **OPS-004 MissingCodeowners** — Add .github/CODEOWNERS to assign review responsibility. Without it, PRs have no automatic reviewer assignment.
- **OPS-005 MissingContribGuide** — Add CONTRIBUTING.md documenting the PR workflow, branch naming, and review process.
- **OPS-007 NoDockerignore** — Add .dockerignore at the project root to keep .git, venv, __pycache__, tests, and secrets out of the build context. Without it, every COPY . sends megabytes of noise to the daemon and risks leaking files into the image.
- **OPS-009 MissingHealthCheck** — Add a HEALTHCHECK instruction so orchestrators can detect a sick container before routing traffic to it. For pure builder stages or one-shot CLIs, suppress this rule per file.
- **SCHEMA-001 MissingTimestamps** — Add created_at and updated_at DateTimeField columns with auto_now_add and auto_now. These are essential for debugging, auditing, and data management.
- **SCHEMA-003 NoStringLengthLimit** — TextField has no length limit in the database. If this field has a natural maximum length (names, titles, codes), use CharField with max_length instead.
- **SEC-001 NoMetaPermissions** — Define explicit permissions in Meta.permissions for fine-grained access control. Django's default add/change/delete/view may not match your authorization needs.
- **SMELL-001 MysteriousName** — Use descriptive names that reveal intent. Single-letter variables obscure meaning.
- **SMELL-010 DataClumps** — Extract these related parameters into a dataclass or named tuple.
- **SMELL-013 Loops** — Use a list comprehension, generator expression, or sum() instead of an accumulation loop.
- **SMELL-014 LazyElement** — A class with a single method that just returns is likely overengineered. Use a function instead.
- **SMELL-017 MessageChains** — Long attribute chains couple the caller to the navigation structure. Consider introducing a helper method.
- **SMELL-021 AlternativeInterfaces** — Define a common Protocol or ABC to unify the interface.
- **SMELL-022 DataClassSmell** — Use @dataclass or a NamedTuple for pure data containers. They provide __init__, __repr__, and __eq__ automatically.
- **SMELL-024 Comments** — High comment density suggests the code should be clearer. Rename variables and extract methods instead of adding comments.
- **SMELL-025 TemporalIdentifier** — Rename to describe the current purpose. If the 'old' version is dead, delete it. If it's current, drop the temporal prefix. For version suffixes, the canonical name belongs to the active implementation.
- **STAB-010 SharedResourcePool** — Scope ThreadPoolExecutors to a single concern, or partition by workload. A single shared pool means a slow consumer can starve every other caller.
- **STAB-011 MissingHealthEndpoint** — Add a /health or /ready route so orchestrators and load balancers can detect failure. A service with no health endpoint is invisible to its operators.
- **STRUCT-013 NoLockFile** — Add a lock file (pip freeze > requirements-lock.txt) to pin exact dependency versions for reproducible builds.
- **STRUCT-020 MissingReturnTypes** — Add return type annotations to public functions for documentation and type-checker support.
- **SVC-002 ChattyIntegration** — Multiple sequential HTTP calls to external services suggest a missing batch or aggregate endpoint. Consolidate into fewer, larger requests.
- **SVC-003 NoAPIVersioning** — Add a version prefix (e.g. '/v1/...') to API routes. Versioned APIs allow safe evolution without breaking existing clients.
- **SVC-006 MissingContractTests** [requests] — A module that talks to an external service is a contract surface. Add a test that exercises the request shape -- even a mock-based one -- so a remote API change cannot pass CI silently.
- **TEST-ARCH-002 PytestNoTestCoverage** [pytest] — Add tests/test_{stem}.py (or tests/<subpath>/test_{stem}.py) so this module has a direct test surface. Some modules legitimately have no tests -- if so, suppress this rule for the file.
- **TEST-SCALE-001 PytestFixtureScope** [pytest] — Add scope='session' or scope='module' to fixtures that create expensive resources (database connections, API clients) to avoid recreating them per test.
- **TEST-STRUCT-001 PytestAssertMessage** [pytest] — Add a failure message to complex assertions: assert condition, 'description'. Messages make test failures easier to diagnose.
- **TEST-STRUCT-002 PytestFixtureDependencyDepth** [pytest] — Flatten the fixture graph: inline trivial dependencies, or compose explicit setup objects in a single fixture. Deep fixture chains make failure diagnosis painful.
