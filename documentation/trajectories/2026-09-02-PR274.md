---
date: 2026-09-02
repo: gaudi
pr: gaudi#274
branch: fix/issue-273-incomplete-run-every-renderer
issues:
  - gaudi#273
session_kind: in-session-pickup
duration: ~75min
reviewer: BLOCK findings=2 tokens=unknown
---

# Trajectory — an incomplete run must read as incomplete in every renderer

## Context

PR #272 made a pack-load failure an incomplete run rather than a clean one, and
its second adversarial review passed with three findings — all the same shape as
the BLOCK that preceded it: the fix reached the consumers that were thought
about and not the ones that were only read past. #273 is those three findings.
`check`'s **text** renderer still printed "Structurally sound" in green over a
failed pack; `_reject_unusable_packs` rendered prose on stdout whatever
`--format` asked for; and `cheat-sheet`, the fourth consumer of the pack
registry, wrote a gutted catalog and exited 0 — which `--check` then certified.
The first two contradict README's own "a broken install cannot report as a clean
project", so they had to land before 0.3.0 is published.

## Trajectory

Finding 1 was a two-line condition. Findings 2 and 3 were design questions.

**Finding 2 was not a rendering bug, it was a layering one.**
`_reject_unusable_packs` ran inside `_run_check`, which does not know the output
format, and it *rendered* — so the obvious fix, threading `output_format` down
into the validator, would have put a `text`/`json`/`github`/`count`-stderr
dispatch inside a function whose job is validation. The rejected alternative was
worse than it looked: `count --format json` wants a `{code: count}` map, not
`check`'s document, so the validator would have needed to know each command's
document shape too.

The shape that removed the problem instead of relocating it: the validator
**returns** the failed packs the caller named and the run proceeds. A pack error
already rides on every `CheckResult` and is already rendered by every format, so
the named-pack case became an ordinary pack error with no renderer of its own.
`engine.check_result` returns early when `pack_names` matches no loaded pack, so
nothing is computed twice. `count` and `report` needed no change at all — they
already exit 2 for any pack error, which is why `count --pack <failed>` was
fixed by deletion.

That left one behaviour with nowhere to live: `check --pack <failed>` exits 2
*without* `--exit-code`, pinned by #272. It is a real distinction — the caller
named that catalog and did not get it — so it is carried explicitly, as
`named_failed` on a `_CheckRun` NamedTuple returned by `_run_check`. The
alternative (drop it, and let `check` follow its usual flag-gated rule) would
have silently regressed a pinned, documented contract to fix an unrelated
finding.

One deliberate loss: the bespoke footer "This is the pack you asked for. It is
installed; it did not load." goes with the bespoke renderer. The ordinary block
says the same thing in the words every other pack error uses, and a second
phrasing of one fact is what made the format bug possible.

**Finding 3's design question was where to refuse.** After `discover_packs` and
before rendering, so that neither the file nor stdout is touched and `--check`
is covered by the same guard. The subtle case is the one that makes it a false
green rather than a wrong file: the broken pack contributes no rules, so the
rendered catalog can be **byte-identical** to the committed one and `--check`
happily reports "up to date". A catalog that was never built from every pack
cannot be certified whatever its bytes say — so the refusal precedes the
comparison, and the test that proves it generates the file healthy first and
then runs `--check` broken.

## What worked

- [process] Writing all 21 tests before any implementation, then running them
  against the unfixed tree — 15 behavioural reds, 6 that passed and were
  therefore labelled controls and pins on the spot, before the fix could make
  the distinction convenient.
- [design] Deleting the special-case renderer rather than teaching it about
  formats — `count` and `report` then needed no code change, and the diff for
  finding 2 is mostly the removal of a branch.
- [tooling] Deriving the mutant list from the *diff* rather than from the guards
  remembered (PR #272's own lesson): eight mutants, including two placement
  mutants (M7 moves the cheat-sheet refusal after `--check`; M8 inverts the
  failed/unknown precedence) that a condition-only list would have missed. M7
  killed exactly one test, which is the test that exists for that placement.
- [process] Splitting the finished tree into per-finding commits by
  reconstructing intermediate file states and running the suite at each — every
  commit is independently green, so a bisect lands on one finding.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [process][trivial] `detect-secrets` rewrote `.secrets.baseline` mid-commit
  (line-number shifts from the README/CHANGELOG edits) and failed the commit
  → remedy: on any PR touching README or CHANGELOG, stage `.secrets.baseline`
  with the docs commit from the start rather than after the hook complains.
- [design][minutes] First sketch threaded `output_format` into
  `_reject_unusable_packs`, which put a four-way format dispatch inside a
  validator and still could not serve `count --format json` → remedy: when a
  fix wants a renderer to learn about its callers, check whether the caller
  already has a renderer for that data — here every format already rendered
  `pack_errors`.

- [process][hours] The adversarial reviewer returned **BLOCK** with two
  findings, and both were the *same* mistake this PR was written to fix. I
  counted renderers by reading the `if/else` I had just edited and stopped at
  two, while `check` alone has three -- the JSON `summary` field is produced by
  `Engine.format_summary`, one call away, and a comment I wrote in this PR
  asserted "the two renderers render the same CheckResult" -> remedy: count a
  claim's renderers by grepping for the *sentence*, not by reading the branch
  in front of you; a claim written in three places is a claim that gets fixed
  in two.
- [functional][hours] "Structurally sound" was also false for a path **no pack
  applies to** -- `check ./docs` over a directory with no Python exited 0 and
  called it sound. I had scoped the whole pickup to "incomplete run" as
  defined by the two records that already existed, and never asked whether a
  third state could reach the same sentence -> remedy: when fixing a false
  green, enumerate every path that reaches the green *string*, not every path
  that sets the known flags.
- [process][minutes] Mutant N8 (delete the Markdown "no language pack applies"
  bullet) **survived**: my report test asserted `"No language pack applies" in
  stdout`, which the *verdict sentence* also satisfies, so the block could lose
  its only bullet and stay green -> remedy: when two strings in one document
  share a prefix, assert on the form unique to the one under test (here the
  Markdown bullet `- **...**`), and trust the mutation, not the test name.
- [process][trivial] Built the commit-splitting script inside an *unquoted*
  bash heredoc, so `"\\n"` in a Python anchor string collapsed to a real
  newline and the anchor never matched -> remedy: any heredoc carrying Python
  source uses `<<'EOF'`, always.

## What was learned

- [design] A false green fixed in one renderer is not fixed. #272 threaded four
  commands and missed the second renderer *inside one of them*; the audit unit
  is the renderer, not the command → promote: doctrine.
- [design] When a validator renders, it has taken a decision that belongs to its
  caller. Returning the finding and letting each command report it in its own
  shape removed the bug and two of the three code paths → promote: doctrine.
- [functional] A drift guard must refuse when it could not build the thing it
  compares, *before* the comparison — a subset build can be byte-identical to
  the correct artifact, so "the file matches" is not evidence the catalog was
  complete → promote: memory.
- [process] A behaviour pinned by a merged PR is a contract even when the fix in
  hand would tidily remove it. `check --pack <failed>` exiting 2 without
  `--exit-code` survived as an explicit field rather than being dropped as a
  side effect → promote: doctrine.

- [design] A third kind of "could not look" earns its own field for the same
  reason `PackError` earned its own record, but it also needs a *precedence*:
  a pack that failed to load is the pack that would have applied, so
  `examined=False` beside a `PackError` must be reported as the broken install,
  never as "no language pack applies" -> promote: doctrine.
- [process] A mutant that survives is worth more than ten that die: N8 was the
  only one that found a real gap, and it found it in a test whose name and
  intent were both correct -> promote: doctrine.

## Tools

- mutation harness (scratchpad, `mutate.py`) [used] — carried over from #272:
  `cp` backup, `__pycache__` cleared between mutants, `python -B`, scoped to the
  five test files that carry the contract. Extended with placement mutants
  (re-insert a deleted block at a later anchor).
- `.venv/bin/python -m gaudi.cli check . --severity error --exit-code` [used] —
  self-dogfood, error-clean before and after.
- per-finding commit reconstruction (scratchpad) [NEW] — builds the intermediate
  `cli.py` / test-file states from the HEAD and final versions so the finished
  work can be committed as three independently-green fixes.

## Re-review cycle 1

The reviewer returned **BLOCK** with two findings at head `2be46e0`, both of
them the same class this PR exists to close.

1. **The JSON `summary` field.** `check --format json` built its summary from
   `Engine.format_summary(findings)`, which knows nothing about skips or pack
   errors, so the document that listed `pack_errors` also said "Structurally
   sound". Fixed at the source: `formats.format_empty_verdict` owns the
   sentence and all three renderers ask it. Seen red on 3 assertions; killed by
   mutants N1 (findings-only summary), N2 (call site drops the run) and N3
   (verdict always sound, 14 tests red).

2. **A path no pack applies to.** `check <dir with no Python>` exited 0 and
   called it structurally sound, because `check_result` returns before any pack
   runs. Fixed by option (a): `CheckResult.examined`, threaded through every
   renderer and every exit gate, with a failed pack taking precedence over the
   "nothing applied" diagnosis. Seen red on 14 behavioural assertions plus 2
   shape reds; killed by mutants N4-N12.

Three tests in `test_report_formats.py` had pinned the old behaviour over an
empty `tmp_path` and called it a "clean dir" -- they asserted the false green
directly. They are updated to the corrected contract rather than deleted, and
the change is disclosed in the PR body and as a CHANGELOG migration note.

## Open threads

- `Engine.check()` and `Pack.check()` return `list[Finding]` and drop skips and
  pack errors by signature. Nothing in `src/` or `scripts/` calls them, but they
  are public API: either they should return a `CheckResult` or they should be
  deprecated in favour of `check_result`. Not decided here.
- `check` in `cli.py` grew again (the text renderer's incomplete branch). The
  renderer still wants extracting to a formatter beside `format_markdown_report`,
  which would have made finding 1 impossible — the two renderers would be one.
- `Engine.format_summary` is now a four-parameter public method. The run it
  summarises is a `CheckResult`; taking one would be the honest signature, and
  is a breaking change this PR did not take on.
- `_reject_unusable_packs` now silently ignores an unknown pack name when
  another named pack failed to load (unchanged from #272, where the exit
  happened first). Reporting both is a small improvement nobody has asked for.

## Cycle 3

The reviewer returned **BLOCK** with three findings at head `2c795de`. Head is
now `486dcb9`, one commit per finding, each independently green.

### Finding 1 — `--pack` skipped the question `examined` answers

`check_result` computed `examined` only on the auto-detect path. With
`pack_names` given it took every *loaded* pack the caller named and never asked
`can_handle`, so `gaudi check ./docs --pack python` ran the Python pack's
project-scope rules over a directory with no Python in it and reported
`examined: true` — "Structurally sound." and exit `0` where the same path under
auto-detection reported an incomplete run and exited `2`. The README, the
CHANGELOG and the `CheckResult.examined` docstring — all written by this diff —
were true on one selection route and false on the other.

**The fix is the smaller shape, not the reviewer's literal suggestion.**
Computing `examined = any(pack.can_handle(path) ...)` while still *running* the
named packs would have produced a run that reports six findings and says nothing
was examined. Selecting packs by name is a filter on the catalog, not an
assertion about the path, so `can_handle` now filters the named packs exactly as
it filters the auto-detected ones, and the two routes cannot disagree about the
same path. `gaudi.toml`'s `packs` list reaches `check_result` by the same
argument and needed no separate code.

**Seen red — each test run individually against the unfixed tree:**

| test | red for |
| --- | --- |
| `…_engine_marks_a_named_pack_that_cannot_handle_the_path_unexamined` | `assert True is False` — `CheckResult(…, examined=True)` carrying 6 findings |
| `test_the_text_renderer_says_nothing_was_examined` | verdict absent; output was the six project-scope findings |
| `test_it_exits_two_at_every_severity[error]` | `assert 0 == 2` |
| `test_it_exits_two_at_every_severity[warn]` | `assert 1 == 2` |
| `test_it_exits_two_at_every_severity[info]` | `assert 1 == 2` |
| `test_the_json_document_records_it` | `assert True is False` (`examined`) |
| `test_the_github_output_annotates_it` | `::error title=Nothing examined::` absent |
| `test_count_exits_two_and_keeps_the_integer_on_stdout` | `assert 0 == 2` |
| `test_report_names_it_in_the_incomplete_run_block` | `assert 0 == 2` |
| `test_the_config_pack_list_takes_the_same_route` | `assert 1 == 2` |

Three controls passed against the unfixed tree and are declared as controls in
the class docstring: `test_a_named_pack_that_handles_the_path_still_examines_it`,
`test_it_still_exits_zero`, and
`test_one_named_pack_that_applies_beside_one_that_does_not_is_examined`
(`--pack python --pack ops` over a Dockerfile — ops applies, so the run looked).

**The config test was vacuous on its first draft and was rewritten before the
fix.** It wrote `[tool.gaudi]` (the table is `[gaudi]`), so the file was never
read — and over a path nothing applies to, both routes agree, so it would have
passed either way. It now runs over the Dockerfile project, which auto-detection
*would* have examined, so only the configured pack list being read can satisfy
it.

### Finding 2 — the lists that name what makes a run incomplete

`check --exit-code`'s help, `count`'s docstring, the exit-code comment in
`check`, and the README's exit-code table each still enumerated two causes after
the third was added. The README's *prose* had been updated to three; its table
row had not, which is the same "fixed in two of the three places it is written"
this PR exists to close.

The three a caller can read back are pinned:
`test_check_help_names_all_three`, `test_count_help_names_all_three` and
`test_the_readme_exit_code_table_names_all_three` assert all three causes
appear, with click's wrapping normalised away. All three were seen red
(`assert 'no installed pack applies' in …`). The fourth site is a source
comment, which no test can hold — disclosed rather than claimed.

### Finding 3 — the verdict colour was unmeasured

`complete = examined and not skipped and not pack_errors` chose green or yellow,
and the comment beside it called green part of the claim, but no test rendered
colour: rich emits no SGR codes when its file is not a terminal, and under
`CliRunner` it never is. `tests/test_verdict_colour.py` swaps the module-level
console for one told to emit colour and asserts on the codes — green for a
complete clean run, never green for a path nothing applied to, a file that could
not be parsed, or a pack that failed to load.

**These are pins, not seen-red guards, and say so in their docstring.** The
colour was already correct; what was missing was anything that would notice if
it stopped being. The control (`…_complete_clean_run_is_green`) is what keeps
the three negative assertions from passing on a console that emitted no colour
at all.

### Mutated

Eight mutants, each applied alone, `__pycache__` cleared between them, every run
under `python -B`, scoped to the seven test files that carry the contract (201
tests). **All eight killed; none survived.**

| # | mutation | tests that went red |
| --- | --- | --- |
| E1 | drop the `can_handle` filter on the named-pack path (the defect) | **10** |
| E2 | invert it (`if not pack.can_handle(path)`) | **12** — the 10 above **plus two controls**, which is what proves the filter is not vacuous |
| E3 | **placement**: filter after the early return, so `examined` is decided before `can_handle` is consulted | **10** |
| P1 | `check --exit-code` help back to two causes | 1 |
| P2 | `count` docstring back to two causes | 1 |
| P3 | README exit-code row back to two causes | 1 |
| C1 | `complete = True` (the mutant that survived 203 tests for the reviewer) | **3** |
| C2 | drop `examined` from the colour predicate only | 1 — the nothing-examined pin, the only state that distinguishes it |

### Every path to the green sentence, re-enumerated

Grepped for the *sentence*, not the flag. `COMPLETE_RUN_VERDICT` is written once
in `formats.py` and returned only by `format_empty_verdict`, which has exactly
three call sites — `cli.py`'s text renderer, `Engine.format_summary` (which the
JSON `summary` and the text counts line both call), and
`format_markdown_report`. All three pass `(skipped, pack_errors, examined)`, and
after finding 1 `examined` is now decided the same way whichever route selected
the packs.

### Gates, on head `486dcb9`

| gate | rc |
| --- | --- |
| `ruff format --check .` | 0 |
| `ruff check .` | 0 |
| `python -m gaudi.cli check src/ --severity error --exit-code` | 0 |
| `python -m gaudi.cli check . --severity error --exit-code` (self-dogfood) | 0 |
| `pytest --tb=short --cov=gaudi --cov-fail-under=60` | 0 — **1341 passed** (1321 at the blocked head, +20), coverage 94% |
| `python -m gaudi.tools.fixture_coverage --strict` | 0 — 150/150 complete |
| `python -m gaudi.cli cheat-sheet --check -o docs/gaudi-rules.md` | 0 |
| `bandit -r src/ -c pyproject.toml` | 0 |

pre-commit ran on each of the three commits and passed.

### What didn't  (cost: trivial | minutes | hours | blocked)

- [process][minutes] `pre-commit run --all-files` reformatted **eleven files
  this branch never touched** and rewrote a doc's trailing whitespace — the
  hook's pinned ruff differs from the venv's, so the repo carries pre-existing
  drift that `.venv/bin/ruff format --check .` calls clean → remedy: run
  pre-commit through `git commit` (staged files only) and reserve `--all-files`
  for a branch whose purpose is that sweep; a repo-wide reformat inside a
  re-review cycle is a different PR.
- [process][trivial] The first draft of the config-route test wrote
  `[tool.gaudi]` and ran over a path both routes already agreed on, so it
  passed against the unfixed tree for two independent wrong reasons → remedy:
  a test for "route B reaches the same code" must be run over an input where
  route A and route B *disagree*, or it measures nothing.

### What was learned

- [design] A selector is not an assertion. `--pack python` chooses a catalog; it
  says nothing about the path, and letting it decide `examined` let the caller
  overrule a fact about the filesystem → promote: doctrine.
- [process] A claim proved on one input route is proved on one input route. The
  `examined` record was threaded through five renderers and three exit gates and
  was still wrong, because every test reached it through auto-detection →
  promote: doctrine.
- [process] "The colour is right" and "nothing would notice if it stopped being"
  are compatible. A comment asserting an invariant that no assertion holds is
  the invariant's whole defence → promote: doctrine.
