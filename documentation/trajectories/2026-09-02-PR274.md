---
date: 2026-09-02
repo: gaudi
pr: gaudi#274
branch: fix/issue-273-incomplete-run-every-renderer
issues:
  - gaudi#273
session_kind: in-session-pickup
duration: ~75min
reviewer: <pending> findings=unknown tokens=unknown
---

# Trajectory — an incomplete run must read as incomplete in every renderer

## Context

PR #272 made a pack-load failure an incomplete run rather than a clean one, and
its second adversarial review passed with three findings — all the same shape as
the BLOCK that preceded it: the fix reached the consumers that were thought
about and not the ones that were only read past. #273 is those three findings.
`check`'s **text** renderer still printed "Structurally sound" in green over a
failed pack; `_reject_unusable_packs` rendered prose on stdout whatever
`--format` asked for; and `cheat-sheet`, the fourth consumer of the pack
registry, wrote a gutted catalog and exited 0 — which `--check` then certified.
The first two contradict README's own "a broken install cannot report as a clean
project", so they had to land before 0.3.0 is published.

## Trajectory

Finding 1 was a two-line condition. Findings 2 and 3 were design questions.

**Finding 2 was not a rendering bug, it was a layering one.**
`_reject_unusable_packs` ran inside `_run_check`, which does not know the output
format, and it *rendered* — so the obvious fix, threading `output_format` down
into the validator, would have put a `text`/`json`/`github`/`count`-stderr
dispatch inside a function whose job is validation. The rejected alternative was
worse than it looked: `count --format json` wants a `{code: count}` map, not
`check`'s document, so the validator would have needed to know each command's
document shape too.

The shape that removed the problem instead of relocating it: the validator
**returns** the failed packs the caller named and the run proceeds. A pack error
already rides on every `CheckResult` and is already rendered by every format, so
the named-pack case became an ordinary pack error with no renderer of its own.
`engine.check_result` returns early when `pack_names` matches no loaded pack, so
nothing is computed twice. `count` and `report` needed no change at all — they
already exit 2 for any pack error, which is why `count --pack <failed>` was
fixed by deletion.

That left one behaviour with nowhere to live: `check --pack <failed>` exits 2
*without* `--exit-code`, pinned by #272. It is a real distinction — the caller
named that catalog and did not get it — so it is carried explicitly, as
`named_failed` on a `_CheckRun` NamedTuple returned by `_run_check`. The
alternative (drop it, and let `check` follow its usual flag-gated rule) would
have silently regressed a pinned, documented contract to fix an unrelated
finding.

One deliberate loss: the bespoke footer "This is the pack you asked for. It is
installed; it did not load." goes with the bespoke renderer. The ordinary block
says the same thing in the words every other pack error uses, and a second
phrasing of one fact is what made the format bug possible.

**Finding 3's design question was where to refuse.** After `discover_packs` and
before rendering, so that neither the file nor stdout is touched and `--check`
is covered by the same guard. The subtle case is the one that makes it a false
green rather than a wrong file: the broken pack contributes no rules, so the
rendered catalog can be **byte-identical** to the committed one and `--check`
happily reports "up to date". A catalog that was never built from every pack
cannot be certified whatever its bytes say — so the refusal precedes the
comparison, and the test that proves it generates the file healthy first and
then runs `--check` broken.

## What worked

- [process] Writing all 21 tests before any implementation, then running them
  against the unfixed tree — 15 behavioural reds, 6 that passed and were
  therefore labelled controls and pins on the spot, before the fix could make
  the distinction convenient.
- [design] Deleting the special-case renderer rather than teaching it about
  formats — `count` and `report` then needed no code change, and the diff for
  finding 2 is mostly the removal of a branch.
- [tooling] Deriving the mutant list from the *diff* rather than from the guards
  remembered (PR #272's own lesson): eight mutants, including two placement
  mutants (M7 moves the cheat-sheet refusal after `--check`; M8 inverts the
  failed/unknown precedence) that a condition-only list would have missed. M7
  killed exactly one test, which is the test that exists for that placement.
- [process] Splitting the finished tree into per-finding commits by
  reconstructing intermediate file states and running the suite at each — every
  commit is independently green, so a bisect lands on one finding.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [process][trivial] `detect-secrets` rewrote `.secrets.baseline` mid-commit
  (line-number shifts from the README/CHANGELOG edits) and failed the commit
  → remedy: on any PR touching README or CHANGELOG, stage `.secrets.baseline`
  with the docs commit from the start rather than after the hook complains.
- [design][minutes] First sketch threaded `output_format` into
  `_reject_unusable_packs`, which put a four-way format dispatch inside a
  validator and still could not serve `count --format json` → remedy: when a
  fix wants a renderer to learn about its callers, check whether the caller
  already has a renderer for that data — here every format already rendered
  `pack_errors`.

## What was learned

- [design] A false green fixed in one renderer is not fixed. #272 threaded four
  commands and missed the second renderer *inside one of them*; the audit unit
  is the renderer, not the command → promote: doctrine.
- [design] When a validator renders, it has taken a decision that belongs to its
  caller. Returning the finding and letting each command report it in its own
  shape removed the bug and two of the three code paths → promote: doctrine.
- [functional] A drift guard must refuse when it could not build the thing it
  compares, *before* the comparison — a subset build can be byte-identical to
  the correct artifact, so "the file matches" is not evidence the catalog was
  complete → promote: memory.
- [process] A behaviour pinned by a merged PR is a contract even when the fix in
  hand would tidily remove it. `check --pack <failed>` exiting 2 without
  `--exit-code` survived as an explicit field rather than being dropped as a
  side effect → promote: doctrine.

## Tools

- mutation harness (scratchpad, `mutate.py`) [used] — carried over from #272:
  `cp` backup, `__pycache__` cleared between mutants, `python -B`, scoped to the
  five test files that carry the contract. Extended with placement mutants
  (re-insert a deleted block at a later anchor).
- `.venv/bin/python -m gaudi.cli check . --severity error --exit-code` [used] —
  self-dogfood, error-clean before and after.
- per-finding commit reconstruction (scratchpad) [NEW] — builds the intermediate
  `cli.py` / test-file states from the HEAD and final versions so the finished
  work can be committed as three independently-green fixes.

## Open threads

- `Engine.check()` and `Pack.check()` return `list[Finding]` and drop skips and
  pack errors by signature. Nothing in `src/` or `scripts/` calls them, but they
  are public API: either they should return a `CheckResult` or they should be
  deprecated in favour of `check_result`. Not decided here.
- `check` in `cli.py` grew again (the text renderer's incomplete branch). The
  renderer still wants extracting to a formatter beside `format_markdown_report`,
  which would have made finding 1 impossible — the two renderers would be one.
- `_reject_unusable_packs` now silently ignores an unknown pack name when
  another named pack failed to load (unchanged from #272, where the exit
  happened first). Reporting both is a small improvement nobody has asked for.
