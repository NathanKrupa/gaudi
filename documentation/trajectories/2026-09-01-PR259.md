---
date: 2026-09-01
repo: gaudi
pr: gaudi#259
branch: feat/count-subcommand
issues:
  - gaudi#228
  - gaudi#256
session_kind: dispatched-overhaul
duration: ~40m
---

# Trajectory — `gaudi count` and the named debt set

## Context

Rung G2b. #228 asked for a counting primitive because two repos had built one
in scripts. #256 supplied the harder half: *which* findings a ratchet should
count. Counting all of them is what made a ratchet payable by deleting a
comment — ~50% churn, ~19% harm across two repos, 896 new named constants in
one quarter.

## Trajectory

Read grantspider's `src/grantspider/dev/boy_scout.py` first (read-only) to
learn its comment-inflation filter. It re-measures a gaudi length finding by
functional code lines and drops it from the ratchet when the real count is
under threshold — a *post-hoc filter over gaudi's output*.

Decided **not** to promote it in that shape. A filter that corrects a rule's
measurement after the fact only helps the consumer that installs it; the rule
still tells everyone else that a well-commented function is too long, and the
cheapest way to satisfy it is still to delete the comments. The root-cause fix
is for SMELL-003 to count functional lines itself, which is a rule change with
its own fixtures — split out rather than bundled here, so this PR stays one
logical change (the counting primitive) and that one stays another (rule
precision, folded into the G5 batch).

`RATCHET_RULE_CODES` and `count_by_code()` live in `services/ratchet.py`
(middle layer, alongside `services/cheat_sheet.py`); the CLI parses flags,
calls the service, prints. `count` would have been the third copy of the
config/engine/pack-validation block, so `_run_check()` was extracted in the
same diff — the repo's duplication threshold is two.

## What worked

- **Committing before mutating**, after G2a's `git checkout` accident. Every
  mutation this time was restored from a file backup, and `git status` after
  the sweep proved the tree clean.
- Deriving the JSON contract from the ratchet's need rather than from
  convenience: requested-but-zero codes stay in the map, because a baseline
  whose key set moves with the findings cannot be compared, and a missing key
  reads as "no findings" when it may mean "the rule did not run".
- Carrying G1's law into the new command: `count` exits 2 on a skip. A ratchet
  comparing a silently-undercounted branch against a complete baseline reads
  the missing findings as *progress* — the same false green, one level up.

## What did not work

- **A test passed against the unfixed tree for a reason unrelated to its
  subject.** `test_a_skip_makes_count_exit_two` was green before `count`
  existed, because click exits 2 on an unknown command. Nothing about the run
  distinguished that from a real pass. It was only caught by asking, of each
  green, *why* it was green — and it is now proven by mutation instead, and
  labelled in the PR body rather than counted among the reds.
- The `TestCountByCode` / `TestRatchetRuleSet` tests were written alongside
  `services/ratchet.py`, so their pre-implementation red would have been an
  ImportError. Declared as mutation-proven, not seen-red.

## Lessons

- When a consumer has patched around a tool, ask whether the patch belongs in
  the tool *in that shape*. A post-hoc filter over the tool's output and a fix
  to the tool's measurement look equivalent from the consumer's seat and are
  not: only one stops every other consumer from paying the same bug.
- A new subcommand makes exit codes ambiguous before it exists — the framework
  already uses them. Any test asserting a non-zero exit from a
  not-yet-implemented command is suspect until mutation says otherwise.
