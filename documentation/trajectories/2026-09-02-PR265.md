---
date: 2026-09-02
repo: gaudi
pr: gaudi#265
branch: feat/own-house-0-3-0
issues:
  - gaudi#256
  - gaudi#225
  - gaudi#226
session_kind: dispatched-overhaul
duration: ~30m
---

# Trajectory — Gaudi runs Gaudi on Gaudi; 0.3.0

## Context

Final rung. #256's hygiene section: the linter's own CI ran the fixture-coverage
tool and the cheat-sheet drift guard and had never pointed Gaudi at Gaudi; the
`gaudi-linter` / `gaudi` name split had already caused a wrong `pip install` in
an estate CI step; two doc defects (#225, #226) were filed and unworked.

## Trajectory

The self-check was the only part with a real risk, and it was cheap to retire:
the five preceding rungs had left the tree at 0 errors, so the gate goes in
green. But green-on-first-run is exactly the state a false gate is
indistinguishable from, so it was proven in three directions before merge —
clean (0), an injected `sys.path.insert` (1), an injected unparsable file (2) —
each observed, not assumed.

The gate is `--severity error`, not the default. The whole release argues that
warn findings are questions and error findings are defects; gating on warn here
would contradict it in the one repo everyone reads to learn what Gaudi thinks.

#225 turned out to be worse than "stale docs": the documented suppression form
`# noqa: gaudi(<CODE>)` does not merely fail, it fails *silently* — the parser
compares the token `gaudi(SEC-012)` against real rule codes, matches nothing,
and suppresses nothing. Anyone who followed the documentation believed they had
suppressed a finding and had not. Replaced with a table of the three forms that
work.

## What worked

- **Proving the new gate could fail, twice, before merging it.** Two injected
  defects, two distinct exit codes, both observed. The estate's own rule is
  that a check ships with the fixture that makes it fail; a CI step's fixture
  is an injected defect and a recorded exit code.
- Writing a **Migration** section rather than only a change list. Three of this
  release's changes alter what a consumer's gate does — a new exit code, a tier
  demotion that shrinks baselines, and a config parent-walk that makes per-app
  copies redundant. A changelog that lists them as features leaves every
  consumer to discover the breakage.

## What did not work

- **`git commit -C ORIG_HEAD` as a fallback in a compound command committed the
  right files under the *previous PR's* message.** It was written as a
  belt-and-braces retry after a hook failure and did precisely the wrong thing
  silently. Caught by reading `git log --oneline -1` — which is the only reason
  it did not ship. Never reuse a message by reference in an unattended retry;
  write the message to a file and pass `-F`.
- The detect-secrets hook rewrites `.secrets.baseline` and fails the commit;
  every affected commit therefore takes two attempts. Expected behaviour, but
  it means `git commit` output must be read in full each time — a truncated
  read cost two commits earlier in the session.

## Lessons

- A green new gate and a broken new gate look identical. The only difference is
  whether anyone made it red on purpose, and that takes two minutes.
- Documentation that describes a suppression, an exemption or a bypass is part
  of the enforcement surface. A wrong `noqa` syntax is not a typo: it is a
  silent no-op that its readers believe worked.
