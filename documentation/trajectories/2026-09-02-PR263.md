---
date: 2026-09-02
repo: gaudi
pr: gaudi#263
branch: feat/project-root-discovery
issues:
  - gaudi#256
session_kind: dispatched-overhaul
duration: ~35m
---

# Trajectory — project-level questions resolve against the project root

## Context

Rung G5a. `gaudi check apps/billing` in a packaged, locked repo reported "no
pyproject.toml" and "no dependency lock file" — true of the directory, false of
the project. `gaudi.toml` had the mirror defect: read only from the exact path
passed to `check`, which is why one estate repo carried six app-scoped copies
of it.

## Trajectory

The underlying conflation was one field doing two jobs: `PythonContext.root`
both anchored relative paths in findings *and* answered "where is the project?".
Splitting it into `root` and `project_root` (with a `project` accessor that
falls back) made both rules a one-line change.

`find_project_root` walks nearest-first so a monorepo member with its own
`pyproject.toml` is its own root. `.git` is in the marker set specifically as a
**stop**: without it the walk escapes the repository and can adopt a stray
`pyproject.toml` from the user's home directory. `find_config_file` is bounded
at the project root for the same reason.

## What worked

- Landing `gaudi/project.py` before running the tests, so they failed on
  behaviour (STRUCT-011 firing on the subdirectory) rather than on
  `ModuleNotFoundError`. Second time this session that sequencing turned a
  worthless red into a real one.
- Pinning both directions: the fix is only correct if STRUCT-011 and STRUCT-013
  **still fire** on a project that genuinely lacks a pyproject or a lock file.
  A false-positive fix that quietly disables the rule is worse than the false
  positive, and two tests exist solely to catch that.

## What did not work

- **`git rebase origin/main` on a branch cut from a previous PR's branch tried
  to replay commits that were already upstream** — the parent PR had been
  *squash*-merged, so its content is on main under a different SHA and git
  could not match them. The rebase stopped in a conflict and left the working
  tree at the pre-parent state, which looked alarmingly like the edits had been
  lost. `git rebase --onto origin/main <parent-branch> <this-branch>` is the
  form that works: it replays only the commits unique to this branch.
- Mid-session the repository's merge settings changed — `--squash` began
  failing with "Squash merges are not allowed on this repository" after five
  PRs had used it. Switched to `--merge`. Worth knowing before a ladder of
  stacked branches: the merge *method* determines whether later rebases can
  match parent commits.

## Lessons

- One field answering two questions is a defect waiting for a caller who needs
  them to differ. `root` meaning both "what I was pointed at" and "where the
  project is" was fine until someone pointed `check` at a subdirectory.
- For a stacked ladder of PRs, `rebase --onto <new-base> <old-base> <branch>`
  is the default form, not plain `rebase`. Plain rebase only works when the
  parent's commits are byte-identical upstream, which squash-merge guarantees
  they are not.
