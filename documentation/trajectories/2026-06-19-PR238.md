---
date: 2026-06-19
repo: gaudi
pr: gaudi#238
branch: session/stab011-root-urlconf
issues: []
session_kind: in-session-pickup
duration: ~25min
---

# Trajectory — STAB-011 root-URLConf awareness

## Context

STAB-011 (MissingHealthEndpoint) pooled route paths across every URLConf in a
project. For Django that produced a false positive: a project has one root
URLConf (named by `ROOT_URLCONF`, e.g. `config/urls.py`) that should own
`/health`, plus many included app `urls.py` files that correctly carry their
own routes and should NOT each expose a health endpoint. Pooling also let an
app's stray `/health` satisfy a root that lacked one. AG/GS pin
`gaudi-linter>=0.2.0`, so the fix ships as 0.2.1.

## Trajectory

Split `check()` by framework:

- **Non-Django (FastAPI/Flask):** unchanged aggregate behavior — one app owns
  every route, so route detection and the health check pool across all files.
- **Django:** evaluate the root URLConf alone. Root resolution order:
  1. `ROOT_URLCONF = "config.urls"` in any settings file — the authoritative
     pointer Django itself uses; module string mapped to a path suffix.
  2. Fallback: the URLConf file that no other URLConf `include()`s.

  A missing `/health` on the resolved root still flags; included app URLConfs
  are never evaluated.

Added three directory fixtures: a root missing `/health` (fires on
`config/urls.py`, not the app file), a root with `/health` plus a health-less
included app (silent), and an include()-only project with no `ROOT_URLCONF`
setting (exercises the fallback heuristic, silent).

## What was learned

- Aggregate-across-files is the right model for single-app frameworks but wrong
  for Django's mount-tree shape; the rule had conflated the two.
- The fixture corpus' vacuous-pass guard doesn't gate STAB-011 (it has no
  `requires_library`; it gates on `detected_libraries & _WEB_LIBRARIES` inside
  `check`), but the Django fixtures still import `django.urls` so library
  detection is real.
- Fixture `.py` files are linted by ruff (F821 not in the `tests/fixtures/*`
  per-file ignore), so URLConf fixtures need real view callables, not bare
  references.

## Open threads

- Release is a separate overseen step: `0.2.1` is bumped in pyproject + CHANGELOG
  but no tag/`gh release create` was run.
