---
date: 2026-09-01
repo: gaudi
pr: gaudi#258
branch: feat/severity-tiers
issues:
  - gaudi#256
session_kind: dispatched-overhaul
duration: ~45m
---

# Trajectory — style tier to info; project-scoped rules sit out single-file runs

## Context

Rung G2a. #256 measured the warn tier at ~50% churn and ~19% harm across two
repos, with STRUCT-021 and CPLX-002 accounting for 79% of one repo's warnings.
Separately, aigranthelper had disabled STAB-011 and SVC-006 repo-wide because
per-file invocation strips the project context those rules need — losing them
on tree-scoped runs too, which is the worst of both.

## Trajectory

Checked the catalog before writing anything: **SMELL-025 was already `info`**,
so only two of the three named demotions were real work. Reporting the issue's
list back as three demotions would have been a small lie in the changelog.

The project-context mechanism needed a place for two facts: a rule declaring it
needs the project (`Rule.requires_project_context`), and a run declaring what it
was pointed at (`PythonContext.single_file`). One predicate,
`rule_has_the_context_it_needs()`, joins them and is called from both
`Pack.check_result` and `PythonPack.check_result` — one definition, two call
sites, no duplicated logic.

## What worked

- Six mutations, one per demotion and one per declaration plus two on the
  guard, rather than arguing from symmetry that a parametrized test must bite
  for its second parameter. Two of the six reddened tests that nothing else
  touched.
- A control test (`test_single_file_run_still_reports_ordinary_rules`) written
  before the guard, so an over-broad exclusion would have failed rather than
  looked like success.

## What did not work

- **`git checkout <file>` to undo a mutation destroyed the real, uncommitted
  edit underneath it.** Two implementation changes vanished silently; the suite
  went green afterwards only because the tests that covered them had also been
  reverted out of relevance. Restore a mutation from a **file backup**, never
  from git, while the work is uncommitted — and commit before mutating at all.
  The full-suite re-run is what caught it.
- **Truncating `git commit` output with `| tail -6` hid a failing hook.** The
  commit never happened; the next command pushed an empty branch and reported
  success. This is the `| tail` prohibition arriving by a side door: the
  pipeline succeeded, the commit did not, and only `git log` disagreed. Read
  hook output in full, or redirect it to a file and read that.
- **The `gaudi-cheat-sheet` pre-commit hook measured the wrong tree.** It is
  `language: system` with `entry: gaudi …`, so it resolves the `uv tool` gaudi
  on PATH, which imports the *primary* checkout — it reported drift against
  rules this worktree had already changed. Exporting `PYTHONPATH` to the
  worktree made it agree. The hook is a false red (and would be a false green
  in the opposite case) for every worktree session; filed for G6.

## Lessons

- Verify the issue's premise against the catalog before implementing it. One of
  three demotions was already done, and the changelog would otherwise have
  claimed work that never happened.
- Any gate that resolves a tool from PATH measures whichever tree that tool
  imports. In a worktree that is the primary checkout unless `PYTHONPATH` says
  otherwise — pre-commit hooks are not exempt from the rule that catches
  pytest.
