---
date: 2026-09-01
repo: gaudi
pr: gaudi#261
branch: feat/err003-swallow
issues:
  - gaudi#256
  - gaudi#260
session_kind: dispatched-overhaul
duration: ~35m
---

# Trajectory — ERR-003 keys on the swallow, not the log level

## Context

Rung G3, and the single highest-value rule change in the overhaul: #256 found
gaudi firing on 0 of 60 recent bug-fix commits, and this is the one rule that
would have seen a real shipped defect (grantspider `7461aa6a`). It missed it
because it keyed on `logger.error` / `logger.exception` and the handler logged
at `warning` — 88% of that repo's swallow population.

## Trajectory

Read the neighbouring ERR rules before touching anything, which changed the
scope. #256's item text says the rule should fire on "log-only at any level
**or bare pass**" — but ERR-004 `ExceptPass` already ships and fires on
`except: pass`. Adding it to ERR-003 would have failed gaudi's own Subsumption
question, so the widening covers levels only and two boundary tests pin that
ERR-001 and ERR-004 keep their ground.

`logging_rules.py` already had `_is_logger_call` over a `_LOG_METHODS_ALL` set.
Rather than copy it into `errors.py`, both now import `is_logger_call` /
`LOG_METHODS` from `ast_helpers.py` — the repo's stated duplication threshold
is two, and this was the second caller.

## What worked

- **Landing the helper extraction first, as a pure refactor, so the rule tests
  could go red on behaviour.** The first red was an `ImportError` for
  `LOG_METHODS`, which proves only that a module is new. Moving the helper
  (behaviour-neutral) and re-running produced the real signal: five level cases
  and the warning canary failing on the assertion, `error` and `exception`
  correctly still green.
- Four mutations, including one (`if not has_raise:`) chosen specifically
  because the first three left the two *scope* tests green. Without it, the
  boundary tests that stop ERR-003 subsuming ERR-001/ERR-004 would have been
  decoration.
- Self-dogfood on gaudi's own tree, which the widening made non-trivial for the
  first time.

## What did not work

- **The self-dogfood hit was real, and the honest response cost a decision.**
  `Engine.discover_packs()` swallows a pack-load failure — meaning a broken
  pack makes `check` report zero findings and exit 0, the same
  "could-not-look-reads-as-found-nothing" class as G1's file skip, one level
  up. A bare `# noqa: ERR-003` would have been the inert-control failure the
  estate has been bitten by: a comment that suppresses the scrutiny that would
  have caught it. Filed as #260 and annotated as a *known defect tracked at
  #260* — the comment states the site is unfixed rather than asserting it is
  safe.

## Lessons

- An issue's own description of a fix can over-reach into a neighbouring rule's
  territory. Read the sibling rules before implementing an item that says "and
  also X"; here X was already shipping, and duplicating it would have made two
  rules fire on one line.
- An ImportError red is a licence to keep going, not a seen-red. The fix is
  usually to land the mechanical half separately so the behavioural half can
  fail for its own reason.
- A self-dogfood hit on the rule you just widened is evidence the widening
  works. Suppressing it silently would have thrown away the rule's first real
  catch.
