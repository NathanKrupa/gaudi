---
date: 2026-09-02
repo: gaudi
pr: gaudi#264
branch: feat/rule-precision
issues:
  - gaudi#256
  - gaudi#245
  - gaudi#214
session_kind: dispatched-overhaul
duration: ~1h
---

# Trajectory — precision pass on five rules the estate worked around

# (companion: PR#263, the project-root half of the same rung)

## Context

Rung G5, split in two. G5a (#263) fixed the two rules that asked project-level
questions of a subdirectory. G5b, here, is five rules each firing on a
by-design pattern, evidenced by the `noqa` accretions and rename-to-dodge
commits #256 collected.

## Trajectory

Fixtures first for all five, all six new pass-cases confirmed red for their
stated reason before any rule changed.

Two of the five needed more thought than "add an exemption":

**SEC-002** — exempting `SET …` wholesale would have opened a hole, since
`SET {name} = 5` interpolates the *identifier*, which is the injection the rule
exists to catch. The exemption requires a literal `SET <identifier>` prefix,
and a new **fail** fixture pins the interpolated-target form. An exemption
without a fail fixture guarding its edge is a hole with a comment on it.

**SMELL-025** — #256 asked to exempt "semantic `new_*` bindings". Implementing
that as "no counterpart name exists in the module" broke the *existing*
`fail_new_function.py`, which has no counterpart and is a deliberate fail case.
The full-suite run caught it. Re-reading the rule's own docstring found the
actual defect: it declares its subject to be "module-level constants /
variables" and then walks **every** assignment in the file. A function local
has no callers, no importers, and dies at the end of the call — `new` there is
the English adjective. The fix makes the rule match its stated scope, which is
narrower than the issue asked for and correct where the issue's proposal was
not.

## What worked

- **Running the full suite, not just the rule under test.** The SMELL-025
  counterpart heuristic passed its own new fixture and broke an existing one
  two directories away. A `-k SMELL-025` run would have shown green for the
  new case and I would have shipped it.
- Eight mutations, one per exemption plus one per *widening* of an exemption.
  M2 and M5 are the ones that matter: they turn an exemption into a hole, and
  each is caught by a fail fixture rather than a pass fixture.
- Checking the rule's docstring against its implementation. Twice now
  (SMELL-025 here, and the `_extract_names` comment) the prose described the
  intended scope correctly and the code was wider. The docstring was the
  specification and the bug report at once.

## What did not work

- **detect-secrets rejected the SEC-003 fixture for the same reason SEC-003
  did** — a path constant named `SECRET_SCAN_SCRIPT`. Reaching for `# pragma:
  allowlist secret` did not work and, more importantly, was the wrong
  mechanism: the repo already registers credential-shaped fixtures in
  `.secrets.baseline`. Matching the surrounding convention took one
  `detect-secrets scan --baseline` and left no inline noise.
- Two commits were lost to reading only the tail of `git commit` output again
  and missing a failed hook. The habit costs a cycle each time; redirect to a
  file and grep for `Failed`.

## Lessons

- When an issue proposes *how* to fix a false positive, verify the proposal
  against the rule's existing fail fixtures before implementing it. The
  fixtures encode intent the issue's author may not have had in view.
- An exemption needs a fail fixture on its far edge, or it is an untested
  widening. Both mutations that turned an exemption into a hole here were
  caught by fail fixtures, not by the pass fixtures the exemption was written
  for.
