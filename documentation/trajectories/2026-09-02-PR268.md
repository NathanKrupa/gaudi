---
date: 2026-09-02
repo: gaudi
pr: gaudi#268
branch: fix/issue-267-exit-code-honors-severity
issues:
  - gaudi#267
session_kind: in-session-pickup
duration: ~1h
reviewer: PENDING findings=0 tokens=unknown
---

# Trajectory — `--exit-code` gates at the severity the caller selected

## Context

`gaudi check <path> --severity warn --exit-code` printed "Found 96 warnings
across 33 files" and exited `0` on 0.3.0. `--exit-code` counted
error-severity findings only, whatever threshold `--severity` named, so a
caller who asked for a warn-level gate got one that could not fail. This is
the false-greens class the 0.3.0 overhaul (#256/#257) set out to close, found
by the OverSteward Lane-O agent during the 2026-09-01 sweep and held for this
lane. The estate itself will not run the combination — OS#429 rules warn
findings reviewer input, not a gate — but the CLI shape is a trap for any
external caller, and the estate's own doctrine is not the CLI's business.

## Trajectory

The issue left the decision open: **honor the threshold** (exit 1 on any
finding at or above `--severity`) or **refuse the combination** (usage error).
Honor, on **principle 3, "Names are contracts"** — *a name commits to what a
thing is, and a misleading name is the most expensive lie a codebase can tell,
because the reader trusts it and acts on it.* `--severity warn --exit-code`
names a warn-level gate; the only reading of that pair a user can hold is the
one the flags spell out. Refusing would make Gaudi surprising to every
external user in order to encode one estate's tiering policy, which the CLI
has no standing to hold. Principle 4, "Failure must be named", says the same
thing from the other side: a gate that reports success over findings it was
asked to gate on is silence about failure.

The implementation is one line, because `_run_check` already filters
`result.findings` to `min_severity` before the CLI ever sees them. So the
gate becomes `if findings: sys.exit(1)` — the run fails exactly when the
report it just printed is not empty. That is self-explaining in a way an
independent severity comparison would not be (principle 13): there is no way
for the exit status and the visible report to disagree.

Two follow-on decisions:

- **Default threshold.** `--severity` defaults to `info`, so bare
  `--exit-code` now gates at `info` and goes red on any finding. The
  alternative — keep the error tier when `--severity` was not typed, via
  Click's `get_parameter_source` — was rejected: it makes two invocations with
  identical reports exit differently based on whether a flag was typed rather
  than on its value, which is exactly the hidden dependence principle 3 warns
  about. Documented as a migration note instead. Every documented invocation
  in this repo, its CI, `CONTRIBUTING.md` and `docs/llm-workflow.md` already
  names `--severity error`, so nothing in the estate moves.
- **`test_a_clean_run_still_exits_zero` (#257) had to move to the error tier.**
  Its `clean_project` fixture is a bare temp directory with one module: no
  `pyproject.toml`, no lock file, no PR template, so the project-scope rules
  report one warning and four infos *about the directory*. The test's name
  claimed "clean"; the project was only error-clean. It is now pinned where it
  is true, and the new module carries a project that is genuinely clean at
  every tier.

Building the fixtures took the longest. Project-scope rules (STRUCT-011,
STRUCT-013, OPS-002..005) fire on the directory, not on any module in it, so a
naive temp directory carries a warn and four infos before the fixture file is
even copied in — which would have made "exactly one WARN finding" a lie. The
test helper writes the six files that satisfy those rules, and four profile
tests pin that the list is still sufficient, so a future project-scope rule
names itself rather than leaking into the exit-code assertions.

## What worked

- [tooling] Probing candidate fixtures through `check --format json` before writing a line of test code — three warn-rule candidates were tried and two were rejected (`@lru_cache` did not fire STAB-004 at all; an `except: pass` shape dragged in a SEC-012 error) in about a minute each.
- [design] Pinning the fixtures' finding profiles as their own test class, declared as pins rather than counted as guards — it converts "the fixture drifted" from a confusing exit-code failure into a named one.
- [design] Gating on the already-filtered `findings` list rather than re-comparing severities — the exit status and the printed report cannot disagree, and the mutation pass confirmed the shorter form is not weaker.
- [tooling] Five mutations rather than three. M4 (gate on unfiltered findings — the natural wrong way to implement "honor the threshold") and M5 (drop exit 2 entirely) each reddened a set that M1–M3 left green, upgrading four "passes" assertions and three skip parameters from controls to guards.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [tooling][minutes] A stale `__pycache__` made a *restored* `cli.py` execute as its mutant, and then made mutation 2 report mutation 1's failure set. Both mutations changed the file by zero bytes (`if findings:` ↔ `if any(... == ...)` reordering, `==` ↔ `!=`) within the same second, so Python's mtime+size invalidation did not fire — the file on disk was provably correct (`diff` clean, `inspect.getsource` correct) while the behaviour was the mutant's. **This is the second time in two days**: PR#257's own trajectory note records it, in this same repo, in this same mutation-pass workflow → remedy: run the mutation pass with `python -B` and clear `__pycache__` between mutants — not merely after the restore, which is what the #257 note advises and what proved insufficient here.
- [process][trivial] The `.py.txt` error fixture was first written with a `# noqa: SEC-004` reflexively carried over from ruff habits — which would have suppressed the very finding the fixture exists to carry. Caught by probing the fixture's profile before writing the test → remedy: none; probing the profile is the control.

## What was learned

- [design] When a flag names a threshold, the gate is that threshold — a flag whose behaviour is narrower than its name is a false green whoever wrote it, and "our house does not use that combination" is a fact about the house, not about the tool → promote: doctrine.
- [tooling] Run a mutation pass with bytecode writing disabled (`python -B`) and clear `__pycache__` between *every* mutant, not only after the restore. Same-size edits — swapping two statements, flipping `==` to `!=` — are exactly the mutations a careful reviewer reaches for, and they are exactly the ones mtime+size invalidation misses → promote: memory.
- [tooling] A fixture that must carry "exactly one finding" has to account for the rules that fire on the *project*, not the file. Verify the fixture's profile empirically before writing the assertions that depend on it, and pin the profile as its own test → promote: lessons.jsonl.
- [process] A test whose name asserts a property the fixture does not have will pass for years under a weak gate and break the moment the gate gets honest. When strengthening a gate breaks an old test, check whether the test was mislabelled before assuming the gate is wrong → promote: lessons.jsonl.

## Tools

- `.venv/bin/python -m gaudi.cli check <dir> --format json` [used] — profiling candidate fixtures to find single-finding shapes.
- `.venv/bin/python -B -m pytest` [used] — mutation pass without bytecode caching.
- `gaudi-fixture-coverage --strict`, `gaudi cheat-sheet --check`, `bandit`, `ruff` [used] — pre-PR gates.

## Open threads

- `config.get("severity", "info")` in `_run_check` is dead code: `severity` always arrives from Click with a default of `"info"`, which is truthy, so the `severity or ...` fallback never reaches the config value. A `[gaudi] severity = "error"` line in `gaudi.toml` is silently ignored. Spotted while reading the threshold path; out of scope here — worth its own issue.
- Nothing in the CLI stops a caller pairing `--exit-code` with `--format github`, where the exit status and the annotations can disagree about which findings mattered. Not a defect today; noted.
