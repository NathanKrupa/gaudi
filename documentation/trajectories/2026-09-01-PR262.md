---
date: 2026-09-01
repo: gaudi
pr: gaudi#262
branch: feat/transaction-io
issues:
  - gaudi#256
session_kind: dispatched-overhaul
duration: ~40m
---

# Trajectory — SA-ARCH-001: network I/O inside a SQLAlchemy transaction block

## Context

Rung G4. DJ-ARCH-004 already asked "is there a network call inside a
transaction?" — of Django's `transaction.atomic()` only. grantspider has zero
`atomic()` blocks and 43 explicit `.commit()`s, so the question was asked of
nothing while two of its transaction incidents shipped (a four-day CRASHED
service; 357 rows of paid model output lost).

## Trajectory

Wrote the fixtures first, as the repo mandates for any rule, and confirmed the
three fail cases red before writing the rule. Extracted `_is_http_call` /
`_attr_root` from `dj_arch.py` into `ast_helpers.py` rather than copying them —
second caller, and the repo's threshold is two.

The design decision was where to stop. `session.begin()` used *without* a
`with` block, a session bound through a factory, a client passed in as an
argument — all real shapes, none tracked. The rule looks at `with`-block bodies
and requires the call's receiver root to be a known HTTP client module. #256's
own evidence is why: aigranthelper disabled STAB-011 and SVC-006 repo-wide
because they fired where they could not see, and a rule that is off catches
nothing at all. A narrow rule that fires only on what it can prove is worth
more than a broad one that gets disabled.

## What worked

- **The mutation sweep caught that my pass fixtures were decoration.** Two of
  the four mutations — treating every `with` block as a transaction, and
  deleting the HTTP method-name check — changed the rule's behaviour and left
  the entire corpus green. Both are real precision branches: `with open(...)`
  around an `httpx.get` is the commonest shape in a SQLAlchemy codebase, and
  `httpx.Client()` inside a transaction holds no lock. Three `pass_*` fixtures
  were added *because the sweep found the gap*, and each now reddens its
  mutation. Without the sweep the rule would have shipped with two unguarded
  branches and a corpus that looked thorough.
- Rebasing onto main before pushing: the two G3 commits the branch was cut from
  were dropped as "already upstream", leaving a one-commit diff.

## What did not work

- **A mutation that produces no observable change is not evidence of a strong
  test — it is evidence of an untested branch, or of a badly chosen mutation,
  and telling those apart takes a second look.** M4's first form (deleting the
  method check) left the receiver check in place, so on the existing fixtures
  nothing could differ. Re-running it against a fixture that isolated the
  branch (`httpx.Client()` inside a transaction) turned it into a real signal.
  A "0 red" mutation must be investigated, never recorded as a pass.

## Lessons

- Fixture sets grown from the fail cases outward are systematically weak on the
  pass side: it is easy to write "the code the rule should catch" and hard to
  enumerate "the code that resembles it and must not fire". The mutation sweep
  is what turns that from judgement into measurement.
- Deciding a rule's scope by asking "what will get this rule disabled?" rather
  than "what could this rule catch?" is the lesson #256 paid for twice.
