---
date: 2026-09-02
repo: gaudi
pr: gaudi#272
branch: fix/issue-260-pack-load-failure-is-a-skip
issues:
  - gaudi#260
session_kind: in-session-pickup
duration: ~90min
reviewer: PENDING findings=0 tokens=unknown
---

# Trajectory — a pack that fails to load is an incomplete run, not a clean one

## Context

`Engine.discover_packs()` swallowed a pack-load failure with `logger.warning`
and continued without the pack. A broken install therefore made `gaudi check`
report **zero findings and exit 0** — the same defect class as the unparsable
file #257 fixed, one level up: not one file the parser could not read, but a
whole rule catalog that never ran. The swallow was already flagged by Gaudi's
own ERR-003 during the 0.3.0 self-dogfood and carried a `# noqa: ERR-003`
pointing at #260. This pickup closes it on the channel #257 built and #268
pinned.

## Trajectory

The design question the issue left open was **where the failure rides**: reuse
`FileSkip`, or add a sibling. `FileSkip` carries `file` and `reason`, and the
`github` formatter turns `file` into an annotation path — putting a pack name
there would emit `::warning file=python` for a file that does not exist, and the
text block would say "Gaudi could not parse them" about something that was never
a file. Principle 3 (*Names are contracts*) settles it: a sibling `PackError`
with `pack` and `error`, carried on `CheckResult` beside `skipped`, rendered
through the same shapes.

Making ERR-003 stop firing was the second decision. The rule keys on a handler
that logs and does not re-raise, so keeping `logger.warning` inside the `except`
would have meant re-suppressing it. The handler now *records* the failure and
`continue`s; the logging moved to a loop over the recorded errors immediately
after. That is not rule-dodging — it is the shape the fix implies: the handler's
job is to capture the failure, and the log is one of three places it is then
reported. ERR-003 no longer fires on `engine.py`, verified by a finding-level
diff against an `origin/main` archive rather than by re-reading the source.

ERR-001 (*broad exception handler*) fires on that same handler and fired on it
before this PR too. It was deliberately **left unsuppressed**: adding a
`# noqa: ERR-001` in the very PR that fixes a swallow would change the warn-tier
dogfood profile in a way a reviewer cannot distinguish from hiding a finding.

Two commands beyond `check` were in scope by the same argument that motivates
the issue. `gaudi count` is the ratchet primitive — a count taken over a missing
rule catalog is an undercount, and a ratchet reads missing findings as progress;
it now exits 2 for a pack error exactly as it does for a skip. `gaudi list-packs`
reported "No language packs installed" over a failed import, sending the reader
to reinstall what is already there.

The first mutation run timed out at nine full-suite runs; scoping the mutant
runs to the six test files that carry the contract brought it under the limit.

## What worked

- [tooling] Diffed the self-dogfood JSON against a `git archive origin/main`
  tree rather than eyeballing counts — it proved ERR-003 was gone, showed the
  four warn/info findings the change adds, and separated "same finding, bigger
  number" from a genuinely new one.
- [design] Reusing #257's channel shape (`format_skips` → `format_pack_errors`,
  `skipped` → `pack_errors`) meant the reporting, the severity-independence and
  the exit-2 precedence all came out symmetric with no new concepts.
- [tooling] The mutation harness restores from a `cp` backup and clears
  `__pycache__` between mutants, running under `python -B` — PR#268's trajectory
  recorded a stale `.pyc` making a *restored* file execute as its mutant.
- [design] Extracting `_print_incomplete` / `_warn_incomplete` when the second
  incomplete-run block appeared kept `check` and `count` from growing two
  near-identical renderers, and cut the added warn-tier smells from 6 to 3.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [tooling][minutes] Nine mutants × a full 22s suite exceeded the 2-minute Bash
  ceiling and the kill left a `.bak` beside a mutated `cli.py` → remedy: scope
  mutant runs to the test files that carry the contract, and check for `*.bak`
  and re-run the suite after any interrupted mutation pass.
- [process][trivial] A migration note first offered `--pack` as a way to
  suppress a pack error; the implementation attaches pack errors to every
  result regardless of the packs named → remedy: pin a documented escape hatch
  with a test in the same commit, or do not document it.

## What was learned

- [design] When a second kind of "could not look" arrives, give it its own
  record rather than overloading the first — `FileSkip.file` would have had to
  hold a non-file, and the GitHub formatter would have emitted a path that does
  not exist → promote: none.
- [design] A rule that fires on a fixed defect is a design signal, not a noqa
  candidate: moving the log out of the handler removed ERR-003 honestly, because
  the handler genuinely stopped being the failure's only record → promote: doctrine.
- [process] Never add a `noqa` for a *different* rule in the PR that fixes a
  swallow — a reviewer cannot distinguish an honest acknowledgement from a
  suppression, so leave the pre-existing finding visible and say so → promote: doctrine.
- [functional] A false green rarely lives in one command: the same missing
  evidence had to be threaded through `check`, `count` and `list-packs`. Grep
  every consumer of the data structure before calling an exit-code fix complete
  → promote: memory.

## Tools

- `git archive origin/main | tar -x` [used] — read-only baseline tree for the
  finding-level dogfood diff, with no `git stash` and no checkout in the worktree.
- mutation harness (scratchpad) [NEW] — applies 9 mutants one at a time, runs the
  contract test files, restores from a `cp` backup, clears `__pycache__`.
- `.venv/bin/python -m gaudi.cli check … --format json` [used] — machine-readable
  dogfood for the before/after finding diff.

## Open threads

- `Engine` now trips SMELL-020 (*11 methods and 0 attributes*) — the class keeps
  all its state private. Either the rule should see private attributes or the
  engine wants splitting; not decided here.
- `check` in `cli.py` is 108 lines and 71 of business logic (ARCH-013,
  SMELL-003), pre-existing and enlarged by this PR. The text renderer wants
  extracting to a formatter, which is a refactor of its own.
- `gaudi report` still says nothing about skips or pack errors — the same
  false-green family, untouched here.
