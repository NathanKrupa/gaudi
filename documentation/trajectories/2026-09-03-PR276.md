---
date: 2026-09-03
repo: gaudi
pr: gaudi#276
branch: docs/fold-unreleased-into-0-3-0
issues: []
session_kind: in-session-pickup
duration: ~25min
reviewer: PASS-WITH-FINDINGS findings=2 tokens=46000
---

# Trajectory — fold the post-bump fixes under 0.3.0 before tagging it

## Context

PR #265 bumped the version to 0.3.0 and dated the CHANGELOG section; #260,
#267 and #273 then landed under `[Unreleased]`. `publish.yml` fires on a
published release, so the tag was never cut, and cutting it as the file stood
would have shipped ~120 lines the CHANGELOG still called unreleased. This PR
is the fold and the re-date, nothing else.

## Trajectory

A script did the move (split `[Unreleased]` into its `### Fixed` and
`### Migration` blocks, append each to the matching 0.3.0 subsection, assert
the subsection set is exactly those two), so the fold could be checked by a
sorted-line diff rather than read. One relative phrase had to be rescoped
because it now sits inside the section it pointed at.

## What worked

- [process] Moving text by script and proving it with a sorted-line diff — the
  reviewer reproduced the same proof independently.
- [process] `--no-issue` on the assembler records that the change closes
  nothing, rather than leaving the issue slot empty.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [process][trivial] `detect-secrets` re-stamps `.secrets.baseline` line
  numbers on every CHANGELOG edit and fails the commit, twice in this PR →
  remedy: on a CHANGELOG-touching commit, `git add .secrets.baseline` in the
  same `git add` from the start; the PR #274 note already said so.
- [design][minutes] Two Migration lists written against different baselines
  re-scoped each other once merged: "`--severity error` is unchanged" (true of
  #267 alone) became false for the release, and "gained exit status 2 — could
  not be parsed" (true when 0.3.0 carried only that cause) became an
  under-enumeration → remedy: when folding relative statements into a larger
  section, re-read every "unchanged" / "gained X because Y" sentence against
  the section's new baseline, not the PR it came from.
