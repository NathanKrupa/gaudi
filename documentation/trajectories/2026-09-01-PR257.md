---
date: 2026-09-01
repo: gaudi
pr: gaudi#257
branch: feat/skip-accounting
issues:
  - gaudi#256
session_kind: dispatched-overhaul
duration: ~1h
---

# Trajectory — skip accounting: a file the parser cannot read is reported and counted

## Context

Rung G1 of the gaudi overhaul (plan: OverSteward#424 comment 5502421940).
Instrument integrity first: every later rung's evidence is only worth the
instrument's sensitivity, and the instrument had a hole. `_parse_file` caught
`SyntaxError` and returned an empty `FileInfo`; nothing downstream knew the
file had never been read. Under `--exit-code` that printed the same `0` a
clean run prints — which is how the estate's local error gate certified ~36
aigranthelper files whose PEP 758 syntax the installed 3.11 interpreter could
not parse.

## Trajectory

Read `engine.py` / `pack.py` / `cli.py` / `formats.py` / `packs/python/parser.py`
before writing anything, to find where the skip is *created* (parser) and every
place it has to survive to (three output formats plus the exit code).

The design question that decided the shape: is a skip a finding? No — a finding
is evidence, a skip is the absence of evidence. If skips were modelled as
findings they would inherit `--severity` filtering and rule overrides, and
`--severity error` (the estate's gate invocation) would hide exactly the thing
the gate needs to know. So a skip travels on its own channel: `FileSkip` in
`core.py`, `CheckResult(findings, skipped)` returned by new `check_result()`
methods on `Pack` and `Engine`, with the old `check()` delegating to it.

`context_skips()` in `pack.py` reads the ledger off a context by duck-typing,
so skip accounting is optional per pack — the ops pack declares nothing and
loses nothing.

Exit-code precedence: `2` (skip) outranks `1` (error findings). A skip says the
report is not exhaustive, which is a fact about the *whole* report, not one
more line in it.

## What worked

- **Fixing the "clean" control fixture before trusting it.** The first draft of
  `clean.py.txt` used `open(path)` and tripped SEC-012, so
  `test_a_clean_run_still_exits_zero` failed pre-fix for a reason that had
  nothing to do with skips. A control that is red for the wrong reason proves
  nothing; it had to be genuinely finding-free before it was a control.
- **Declaring the three non-guards as controls** instead of quietly counting
  14 red tests. Only 11 of the 14 bit; the other three assert the absence of
  false positives and are green either way by construction.
- Three mutations (skip exit `2`→`1`; parser drops the `SyntaxError` skip;
  parser drops the unreadable-file skip) each reddened a distinct, expected
  set — confirming no guard was decoration.

## What did not work

- **The PEP 758 fixture had to be abandoned.** The obvious negative fixture is
  the exact estate defect — `except OSError, ValueError:` — but gaudi's own CI
  matrix runs 3.10 through 3.14, and that syntax is *valid* on 3.14. The
  fixture would have passed on one leg of the matrix and failed the test that
  depends on it. Replaced with syntax invalid on every version, with the
  reasoning recorded in the fixture's own header so the next reader does not
  "improve" it back.
- **A stale `__pycache__` made a restored file execute as its mutant.** After
  reverting mutation 3 by `cp`ing a backup over `parser.py`, the suite still
  showed one red — and the traceback read `sys.exit(2)` … `SystemExit: 1`,
  source and bytecode disagreeing in the same frame. Ten minutes went into
  chasing a phantom logic bug. **After any mutation-and-restore cycle, clear
  `__pycache__` before believing the re-run** — otherwise the "restored" green
  or red is measuring the mutant, which is a false result in either direction.

## Lessons

- A skip is not a low-severity finding. Modelling "could not look" as a weak
  form of "found something" hands every consumer a filter that hides it — and
  the consumer most likely to filter (`--severity error`, the CI gate) is
  exactly the one that must not.
- A negative fixture that depends on interpreter version cannot pin a defect in
  a project whose CI spans versions. Pin the *class* with something universal
  and record why in the fixture.
- Mutation testing needs its own hygiene: a restore is not a restore until the
  bytecode cache agrees.
