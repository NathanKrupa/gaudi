---
date: 2026-09-02
repo: gaudi
pr: gaudi#270
branch: fix/issue-266-cheat-sheet-hook-own-checkout
issues:
  - gaudi#266
session_kind: in-session-pickup
duration: ~60min
reviewer: PENDING findings=0 tokens=unknown
---

# Trajectory — the cheat-sheet drift guard measures the checkout being committed

## Context

`.pre-commit-config.yaml` declared the drift guard as `entry: gaudi cheat-sheet
--check -o docs/gaudi-rules.md` with `language: system`, so it resolved `gaudi`
from `PATH`. During the 0.3.0 overhaul (#256) that produced a false red in a
worktree; #266 filed it, noting the same mechanism produces a false green when
the PATH binary happens to agree with its own stale artifact. CLAUDE.md (PR
#265) documented an exported `PYTHONPATH` as the workaround. This pickup removes
the need for it.

## Trajectory

The defect reproduced on the first try and was worse than the issue described.
This machine carries `~/.local/bin/gaudi` at **0.2.2** — a `uv tool` install
predating the 0.3.0 rule overhaul. Perturbing ARCH-010's
`recommendation_template` in the worktree and running the hook with `PYTHONPATH`
unset produced a nine-line diff of rules that had been *re-tiered* between 0.2.2
and 0.3.0, and the perturbation itself appeared nowhere in it. The guard was not
merely measuring the wrong tree; it was structurally incapable of seeing the tree
being committed. That is the false-green mechanism in its plainest form.

Of the two fixes the issue floats, `language: python` +
`additional_dependencies` was rejected: pre-commit keys that cached environment
on the dependency list, so a checkout whose *source* changed while its declared
dependencies did not is served the cached install — reproducing the false green
the PR exists to remove. Proving a cache cannot serve a stale tree is a harder
obligation than avoiding the cache, so the OverSteward `scripts/lint/` pattern
was taken instead: derive the repo root from `__file__`, put `<root>/src` at
`sys.path[0]`, and delegate to `gaudi.cli` in-process. That is checkout-correct
by construction rather than by configuration.

Two design questions the issue does not settle were decided here.
**Interpreter:** the script prefers this checkout's own `.venv` and re-executes
into it once (guarded by an env sentinel so a missing dependency cannot loop),
because a contributor's bare `python3` may lack `click`; when the import still
fails it exits **2** with the reason on stderr, never 0. **Working directory:**
the artifact path stays relative and the script `chdir`s to the repo root, which
resolves the same file an absolute path would while keeping the CLI's printed
remediation commands short and copy-pasteable — an absolute path wrapped them
across three lines in rich's console output.

The file landed in `scripts/lint/`, not `scripts/dev/`. `pyproject.toml` excludes
`scripts/dev` from bandit on the grounds that it holds OverSteward-canonical
byte-copies; a gaudi-specific script placed there would have inherited a blind
spot meant for something else. Moving it into bandit's coverage immediately
surfaced a real B606 that the `scripts/dev` location had hidden — the exclusion
is load-bearing, and so is staying out of it.

## What worked

- [tooling] Reproducing the defect *before* writing any fix, with `PYTHONPATH` explicitly unset — the stale 0.2.2 PATH binary made the failure far more vivid than the issue's description, and grepping the pre-fix diff for the perturbation (0 hits) proved blindness rather than mere disagreement.
- [process] Running a mutation pass over the new tests instead of trusting green — seven of eight mutations bit, and the survivor (`os.chdir(root)` deleted) named a genuinely untested load-bearing line, which then got its own test.
- [tooling] Running `bandit` and `gaudi check` against the *new file's directory* rather than only `src/` — both the bandit exclusion of `scripts/dev` and a dead `# noqa` were found this way, neither of which the shipped gate suite would ever have shown.

## What didn't  (cost: trivial | minutes | hours | blocked)

- [tooling][minutes] `# noqa: STRUCT-010` was copied from the OverSteward original and was dead here — gaudi's own STRUCT-010 already exempts self-locating `__main__` bootstraps, and ruff additionally warned the directive was unparseable → remedy: check that a suppression is load-bearing by deleting it and re-running the tool, the same way a test is proven by mutation.
- [tooling][minutes] `# noqa: BLE001, ERR-001 - reported loudly` silently failed to suppress ERR-001: gaudi's `_NOQA_RE` captures the rest of the comment and splits it on commas, so trailing prose becomes bogus rule codes → remedy: gaudi noqa comments carry codes only; put the justification on the line above.
- [tooling][trivial] The worktree's `.venv` symlink shows as untracked because `.gitignore` says `.venv/`, which matches directories only → remedy: none taken here (out of scope); every path was staged explicitly, never by a blanket stage of the tree.

## What was learned

- [design] A guard that resolves its subject from `PATH` cannot be trusted about *any* checkout, including one it happens to be right about — derive the subject from `__file__` so correctness is structural rather than environmental → promote: doctrine.
- [process] A cached hook environment keyed on a dependency list cannot detect a source-only change, so `language: python` is the wrong shape for a guard whose subject is the repo's own source → promote: lessons.jsonl.
- [tooling] A directory-wide linter exclusion is a claim about what lives in that directory; putting a file of a different kind there silently borrows the exemption → promote: doctrine.
- [tooling] A suppression comment is untested code until it is deleted and the tool re-run — an ineffective `# noqa` and a load-bearing one look identical in the diff → promote: memory.

## Tools

- `pre-commit run gaudi-cheat-sheet --all-files` [used] — the three negative-fixture runs, with `env -u PYTHONPATH`.
- `scripts/lint/cheat_sheet_check.py` [NEW] — the checkout-correct drift guard.
- `bandit -r src/ scripts/ -c pyproject.toml` [used] — surfaced B606 once the file left the excluded directory.
- `scripts/tools/generate_tool_registry.py` [used] — regenerated `data/tool_registry.md` for the new script.

## Open threads

- `data/tool_registry.md` had been stale since 2026-05-26; regenerating it for this PR also added the pre-existing `scripts/dev/secret_scan.py` row. Incidental but correct output of an auto-generated artifact.
- `.gitignore`'s `.venv/` does not match the `.venv` *symlink* that the documented worktree flow creates, so every gaudi worktree shows it as untracked. Worth a one-line fix in a separate PR.
