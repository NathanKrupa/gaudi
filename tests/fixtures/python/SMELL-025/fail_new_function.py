# ABOUTME: Fixture for SMELL-025 — temporal marker in function name.
# ABOUTME: "new" in a function name is relative to nothing and decays on landing.


def new_billing_handler(request):
    return request
